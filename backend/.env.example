# Variables de entorno para el backend
# Renombrar este archivo a .env y colocar en la raíz del proyecto backend

# Configuración de Django
DEBUG=True
SECRET_KEY=django-insecure-p+r@39_65^olvypfbhony5*=x4c+#1553jx9rk!@h=!1+88xib

# URLs de la aplicación
BASE_URL=http://127.0.0.1:8000/api
FRONTEND_URL=http://localhost:5173

# Configuración JWT (opcional: usar un secreto distinto al SECRET_KEY)
JWT_SECRET=your-jwt-secret-here
JWT_EXP_DELTA_SECONDS=86400  # 24 horas

# Configuración de Base de datos (opcional: usar PostgreSQL en lugar de SQLite)
# DATABASE_URL=postgres://username:password@localhost:5432/evaltech

# Configuración de Email (opcional)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=user@example.com
# EMAIL_HOST_PASSWORD=password
# EMAIL_USE_TLS=True